node_modules/
dist/
.wrangler/
.dev.vars
.dev.vars.*
.env
.env.*
*.log
.DS_Store

# Agent worktrees. A second copy of every source file confuses anyone
# reading the repository, and vitest counts its tests twice.
.claude/
.evals/

# Einreichungsinterne Dokumente (Checkliste, Backlog, Prompts, Notizen).
# Sie handeln von der Einreichung, nicht vom Produkt.
notes/
